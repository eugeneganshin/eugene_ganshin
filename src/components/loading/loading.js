import React from 'react';

const styles = {
	backgroundColor: '#fffb96',
	width: '100%',
	height: '100vh',
	display: 'flex',
	justifyContent: 'center',
	alignItems: 'center',
};

const Loading = () => {
	return (
		<div style={styles}>
			<p>...</p>
		</div>
	);
};

export default Loading;
